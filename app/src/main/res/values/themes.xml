<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash Screen Theme -->
    <style name="Theme.Project.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FF23272A</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Project</item>
        <item name="android:forceDarkAllowed">false</item>
    </style>

    <!-- Base application theme. -->
    <style name="Theme.Project" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:forceDarkAllowed">false</item>
    </style>
</resources>